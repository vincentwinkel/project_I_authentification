<!DOCTYPE html>
<html>
	<head>
		<title>Connexion</title>
		<%= settings.stylesheet; %>
	</head>
	<body>
		<center>
			<h1>Connexion</h1>
			Service d'authentification
			<div class="wrapper">
				<form action="<%= (is_empty(settings.origin))?"/sessions":"/apps/sessions"; %>" method="post">
<% if (!is_empty(settings.origin)) then %>
					<span class="grey">Vous voulez acc&eacute;der &agrave; l'addresse <%= settings.origin; %></span><br />
					<br />
					<input type="hidden" name="origin" value="<%= settings.origin; %>" />
					<input type="hidden" name="aid" value="<%= settings.aid; %>" />
<% end %>
					<table border="0">
						<tr>
							<td><label for="login">Pseudonyme : </label></td>
							<td>
								<input<% if (form_errors["login"]) then %> class="error"<% end %> type="text" id="login" name="login" size="30" value="<%= form_values["login"]; %>" />
								<% if (form_errors["login"]) then %><span class="msg_error"><%= form_errors["login"]; %></span><% end %>
							</td>
						</tr>
						<tr>
							<td><label for="password">Mot de passe : </label></td>
							<td>
								<input<% if (form_errors["password"]) then %> class="error"<% end %> type="password" id="password" name="password" size="30" />
								<% if (form_errors["password"]) then %><span class="msg_error"><%= form_errors["password"]; %></span><% end %>
							</td>
						</tr>
						<tr><td></td><td><input type="submit" name="commit" value="Valider" /></td></tr>
					</table>
				</form>
				Ou bien <a href="/users/new">s'inscrire</a>
			</div>
		</center>
	</body>
</html>

